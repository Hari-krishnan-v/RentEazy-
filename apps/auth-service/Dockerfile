# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @renteazy/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t renteazy-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system renteazy-auth-service && \
          adduser --system -G renteazy-auth-service renteazy-auth-service

COPY apps/auth-service/dist renteazy-auth-service/
COPY apps/auth-service/package.json renteazy-auth-service/
RUN chown -R renteazy-auth-service:renteazy-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix renteazy-auth-service --omit=dev -f install

CMD [ "node", "renteazy-auth-service" ]
